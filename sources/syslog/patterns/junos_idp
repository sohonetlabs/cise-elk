JUNOS_IDP RT_IDP: (?<Event>\w+): IDP: at (?<seconds>\d+), (?<Attack Type>\w+) \w+ \w+ <%{IP:src_IP}/(?<src_port>\d+)->%{IP:dst_IP}/(?<dst_port>\d+).*application (?<aplication>.*) by rule (?<rule_id>\d+) of rulebase IPS in policy (?<policy_name>.*)\. .* action=(?<policy_action>\w+)\, threat-severity=(?<threat_severity>\w+), name=(?<threat_name>.*)\, NAT <(?<NAT>.*).\, time-elapsed=(?<time_elapsed>\d+)\, inbytes=(?<in_bytes>\d+)\, outbytes=(?<out_bytes>\d+)\, inpackets=(?<in_packets>\d+)\, outpackets=(?<out_packets>\d+)\, intf:(?<source_interface>.*)->(?<destination_interface>.*)\, packet-log-id: (?<packet_log_id>\d+)\, alert=(?<alert>\w+)\, username=(?<username>.*)\, roles=(?<roles>.*) and misc.*
